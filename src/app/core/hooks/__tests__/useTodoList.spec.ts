import { useTodoList } from "../useTodoList";

describe("app/core/hooks/useTodoList", () => {
  describe("useTodoList()", () => {
    it("should be defined", () => {
      expect(typeof useTodoList).toBeDefined();
    });
  });
});
